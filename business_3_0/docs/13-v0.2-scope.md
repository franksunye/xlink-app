# Web v0.2 范围（POC 波次 2）

> **状态**：已发布（Git tag `v0.2.0`；大项以本快照为准，修正走 patch 或 v0.3 新范围文件）  
> **日期**：2026-05-14  
> **前置真源**：[12-domain-glossary.md](./12-domain-glossary.md)（对象与字段语义）；[09-v0.1-scope.md](./09-v0.1-scope.md) 仅描述 v0.1，**不**承载 v0.2 条目。

## 1. 目标

在 v0.1（Next + BFF + Mock、管家主路径、缓存/SW 基线）之上，完成 **波次 2** 最小竖切：**一条 cloud 只读链路**经 **Adapter + BFF** 进入 Web，并在工单详情增加与现网工单语义方向一致的 **流程节点 / 阶段只读占位**；默认部署 **不因未配置 cloud 而失败**（Mock 与降级路径可验收）。

## 2. 与 v0.1 的关系

| 维度 | v0.1 | v0.2 |
| --- | --- | --- |
| 数据 | 全 Mock | Mock 为主；可选 **cloud 只读**（环境开关） |
| BFF | Mock 直出 | 工单列表/详情读路径经 **统一 Adapter 抽象** |
| UI | 列表/详情主路径 | 详情增加 **只读节点/阶段** 区块（不接完整表单引擎） |

## 3. 范围内

### 3.1 读链路（波次 2 核心）

1. **选定 cloud 只读族（主联调对象）**：现网 `cloud` 模块 `WorkOrderController` 下 **`/basic/workOrder/query`**（分页列表，`FlipInfo<WorkOrder>` JSON）。  
2. **同一 Adapter 内、详情必需子路径**：**`/basic/workOrder/findById`**（`ReturnStatus`，`data.workOrder` + `data.workflowNode` 等），用于列表 id 打开详情后与 cloud 数据一致；仍属 **只读**，不写库。  
3. **Web 工程**：在 `business_3_0/web/` 增加 **读模型 Adapter**（Mock 实现 + Cloud 实现），由环境变量 **`USE_CLOUD_READ`**（`1` / `true` 为开）与 **`XLINK_CLOUD_READ_BASE_URL`**（origin + 应用前缀，如 `https://host/app`）控制；关闭或未配置 base URL 时 **仅 Mock**。  
4. **BFF**：`/api/work-orders` 与 `/api/work-orders/[id]` 在启用 cloud 时经 Adapter 拉取；失败或非 2xx 时 **降级为 Mock**，响应头附带 **`X-Xlink-Read-Source: mock-fallback`**（便于联调区分）。cloud 请求 **转发浏览器 Cookie** 至 cloud（与现网会话一致；Mock 登录会话与 cloud 会话未打通时期望降级）。  
5. **契约**：BFF 对外 JSON 仍使用 [12](./12-domain-glossary.md) 约定字段（`WorkOrder`、`activities` 等）；cloud 字段仅在 Adapter 内映射，**禁止**页面直接使用 cloud 原始键名。

### 3.2 UI 壳（与 [08-milestones](./08-milestones.md) 切片 2 对齐）

- 工单详情页在首屏信息区下方增加 **「流程节点（只读）」** 占位区：展示 `readonlyWorkflowNodes`（Mock 为静态演示；cloud 为 `findById` 映射的简化节点序列）。  
- **不接**完整低代码表单引擎、不接节点表单提交。

### 3.3 非功能

- 沿用 [07-nonfunctional-and-poc.md](./07-nonfunctional-and-poc.md)：新增/变更 BFF 路径仍为 **`Cache-Control: private, no-store`**；不扩大 SW 对 `/api` 的缓存面。

## 4. 范围外

- 支付、签约、电话等 **真实外部集成**。  
- cloud **写路径**（派单、改状态、归档等）。  
- **`/projects/[id]`** 项目详情路由（与 v0.1 决策一致，延续不写）。  
- 完整 FSM 配置台、流程编排编辑。

## 5. 成功标准（验收分档）

| 档位 | 条件 | 说明 |
| --- | --- | --- |
| **A — 接 cloud** | `USE_CLOUD_READ=1` 且 `XLINK_CLOUD_READ_BASE_URL` 指向可登录 dev；浏览器携带 **有效 cloud 会话 Cookie** | 列表/详情数据来自 cloud；详情可见 **只读节点区**（有数据时自 cloud 映射） |
| **B — 契约完成（Mock-only）** | 未开 cloud 或 cloud 不可用 | 行为与 v0.1 一致 + **Adapter 代码路径在 CI 可构建**；详情仍展示 **静态只读节点演示**；无白屏 |

**出口**：至少 **B** 全过；**A** 在 dev 就绪后由联调同学在同一验收脚本上勾选（见 [web/docs/acceptance-v0.2.md](../web/docs/acceptance-v0.2.md)）。

## 6. 与 [06-evolution-and-poc-waves.md](./06-evolution-and-poc-waves.md) 波次 2 对照

| 06 波次 2 要点 | 本版本落点 |
| --- | --- |
| 选定只读接口 | `query` + `findById`（工单只读族） |
| 契约 / Adapter | `lib/work-order-cloud-read.ts` + BFF 调用 |
| 与 business 行为对照（读链路） | 节点区对照 `business` 服务工单「阶段感」；细粒度对齐在 flows 无阻塞后迭代 |

## 7. 候选接口（已决）

| 候选 | 取舍 |
| --- | --- |
| 工单列表 `query` | **采用**（主入口，依赖最少） |
| 当前登录用户 profile | 不采用为首条（与工单 UI 竖切弱相关） |

## 8. cloud 前置与风险

- **dev 基 URL、网关路径、鉴权（Cookie 等）** 以运维/后端配置为准；未就绪时 **不阻塞** v0.2 发版，以 **档位 B** 验收。  
- Mock 登录与 cloud 会话 **分离**时，开启 `USE_CLOUD_READ` 将大概率触发 **mock-fallback**，属预期。
