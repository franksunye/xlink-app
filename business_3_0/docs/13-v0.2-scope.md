# Web v0.2 范围（POC 波次 2）

> **状态**：已发布（Git tag `v0.2.0`；大项以本快照为准，修正走 patch 或 v0.3 新范围文件）  
> **日期**：2026-05-14  
> **前置真源**：[12-domain-glossary.md](./12-domain-glossary.md)（对象与字段语义）；[09-v0.1-scope.md](./09-v0.1-scope.md) 仅描述 v0.1，**不**承载 v0.2 条目。

## 1. 目标

在 v0.1（Next + BFF + Mock、管家主路径、缓存/SW 基线）之上，完成 **波次 2** 最小竖切：**一条 cloud 只读链路**经 **Adapter + BFF** 进入 Web，并在工单详情增加与现网工单语义方向一致的 **流程节点 / 阶段只读占位**；默认部署 **不因未配置 cloud 而失败**（Mock 与降级路径可验收）。

## 2. 与 v0.1 的关系

| 维度 | v0.1 | v0.2 |
| --- | --- | --- |
| 数据 | 全 Mock | Mock 为主；可选 **cloud 只读**（环境开关） |
| BFF | Mock 直出 | 工单列表/详情读路径经 **统一 Adapter 抽象** |
| UI | 列表/详情主路径 | 详情增加 **只读节点/阶段** 区块（不接完整表单引擎） |

## 3. 范围内

### 3.1 读链路（波次 2 核心）

1. **选定 cloud 只读族（主联调对象）**：与 cloud_ui `WorkOrders.vue` 一致：**`POST`** **`basic/workOrder/query.do`**，`Content-Type: application/x-www-form-urlencoded`，分页参数 `page` / `rows` / `sortField` / `sortOrder` 及与列表页一致的 **`in:state|integer#and=1`**；响应体为 **`FlipInfo`** JSON（根级 **`data`** 为工单数组）。  
2. **同一 Adapter 内、详情必需子路径**：**`POST`** **`basic/workOrder/findById.do`**，body `id=<工单_id>`；响应为 **`ReturnStatus`**（`status===1` 时 **`data.workOrder`** + **`data.workflowNode`**）。仍属 **只读**，不写库。  
3. **Web 工程**：在 `business_3_0/web/` 增加 **读模型 Adapter**（Mock 实现 + Cloud 实现），由 **`USE_CLOUD_READ`** 与 **`XLINK_CLOUD_READ_BASE_URL`** 控制；**Base URL 须与 cloud_ui `rootUri` 对齐**（通常含 **`/api`**，例如 `http://host:8070/api`，无尾斜杠）。关闭或未配置 base URL 时 **仅 Mock**。  
4. **BFF**：`/api/work-orders` 与 `/api/work-orders/[id]` 在启用 cloud 时经 Adapter 拉取；失败或非 2xx 时 **降级为 Mock**，响应头 **`X-Xlink-Read-Source: mock-fallback`**。cloud 请求 **转发浏览器 Cookie**；鉴权与 cloud_ui 一致时须带 **`X-Auth-Token`**（优先级：**服务端环境变量 `XLINK_CLOUD_READ_AUTH_TOKEN`** > 入站请求头 **`x-xlink-cloud-token`**；浏览器可将 token 写入 **`localStorage['xlink_cloud_read_token']`**，由 `fetchJson` 自动带到 BFF）。  
5. **契约**：BFF 对外 JSON 仍使用 [12](./12-domain-glossary.md) 约定字段（`WorkOrder`、`activities` 等）；cloud 字段仅在 Adapter 内映射，**禁止**页面直接使用 cloud 原始键名。

### 3.2 UI 壳（与 [08-milestones](./08-milestones.md) 切片 2 对齐）

- 工单详情页在首屏信息区下方增加 **「流程节点（只读）」** 占位区：展示 `readonlyWorkflowNodes`（Mock 为静态演示；cloud 为 `findById` 映射的简化节点序列）。  
- **不接**完整低代码表单引擎、不接节点表单提交。

### 3.3 非功能

- 沿用 [07-nonfunctional-and-poc.md](./07-nonfunctional-and-poc.md)：新增/变更 BFF 路径仍为 **`Cache-Control: private, no-store`**；不扩大 SW 对 `/api` 的缓存面。

## 4. 范围外

- 支付、签约、电话等 **真实外部集成**。  
- cloud **写路径**（派单、改状态、归档等）。  
- **`/projects/[id]`** 项目详情路由（与 v0.1 决策一致，延续不写）。  
- 完整 FSM 配置台、流程编排编辑。

## 5. 成功标准（验收分档）

| 档位 | 条件 | 说明 |
| --- | --- | --- |
| **A — 接 cloud** | `USE_CLOUD_READ=1`、Base URL 正确，且具备 **cloud_ui 同款鉴权**（`X-Auth-Token` 与/或 cloud 域 Cookie） | 列表/详情 **`X-Xlink-Read-Source: cloud`**；详情 **只读节点区** 有数据时与 `findById` 映射一致 |
| **B — 契约完成（Mock-only）** | 未开 cloud 或 cloud 不可用 | 行为与 v0.1 一致 + **Adapter 代码路径在 CI 可构建**；详情仍展示 **静态只读节点演示**；无白屏 |

**出口**：至少 **B** 全过；**A** 在 dev 就绪后由联调同学在同一验收脚本上勾选（见 [web/docs/acceptance-v0.2.md](../web/docs/acceptance-v0.2.md)）。

## 6. 与 [06-evolution-and-poc-waves.md](./06-evolution-and-poc-waves.md) 波次 2 对照

| 06 波次 2 要点 | 本版本落点 |
| --- | --- |
| 选定只读接口 | `query` + `findById`（工单只读族） |
| 契约 / Adapter | `lib/work-order-cloud-read.ts` + BFF 调用 |
| 与 business 行为对照（读链路） | 节点区对照 `business` 服务工单「阶段感」；细粒度对齐在 flows 无阻塞后迭代 |

## 7. 候选接口（已决）

| 候选 | 取舍 |
| --- | --- |
| 工单列表 `query` | **采用**（主入口，依赖最少） |
| 当前登录用户 profile | 不采用为首条（与工单 UI 竖切弱相关） |

## 8. cloud 前置与风险

- **dev 基 URL** 必须与 cloud_ui 请求的 API 根一致（常见 `http(s)://<host>:<port>/api`）。  
- **鉴权**：现网 cloud_ui 对业务接口默认带 **`X-Auth-Token`**（见 `cloud_ui/src/common/common.js`）；仅转发 Cookie 往往 **不足**。联调可任选：**BFF 环境变量 `XLINK_CLOUD_READ_AUTH_TOKEN`**、请求头 **`x-xlink-cloud-token`**、或浏览器 **`localStorage['xlink_cloud_read_token']`**（由 Web `fetchJson` 转发为 `x-xlink-cloud-token`）。  
- 未就绪时 **不阻塞** 发版，以 **档位 B** 验收；脚本 **`npm run verify:cloud-read`**（需上述 env）用于在 **可达 dev** 上复验 HTTP 契约与列表/findById 链。  
- Mock 登录与 cloud 会话 **分离**时，若未配置 token，将 **mock-fallback**，属预期。
