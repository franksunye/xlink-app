# 项目维护待办事项清单 (Project Maintenance TODO List)

本文档用于追踪 Consumer App 已识别的技术债、缺陷和维护任务。

## 🔴 高优先级 (缺陷与失效接口)

- [ ] **修复勘察报告 (Survey) API 500 错误**:
    - **问题**: `pages/index/index.vue` 和 `subpackages/serviceAppointment/files.vue` 仍在使用旧路径 `biz/extForm/query/7971040108398070730.do`，该路径会返回 500 错误。
    - **任务**: 重构这些页面，改用健康的现代接口 `basic/survey/querySurvey` (POST)。
- [ ] **同步文档路径**:
    - **问题**: `API.md` 中的部分路径已失效（如：我的优惠券、卡券中心、巡检报告）。
    - **任务**: 根据代码中 `common.osg.ajax` 的实际调用，对 `API.md` 中的所有路径进行审计，确保 100% 准确。

## 🟡 中优先级 (技术债)

- [ ] **抽象硬编码 ID**:
    - **问题**: `tenantId` ('1340835581184723420') 和 `parentId` ('4417047658598871409') 在多个组件中被硬编码（如 `classification.vue`, `my.vue`, `index.vue`）。
    - **任务**: 将这些 ID 提取到全局配置文件或常量定义中，或者通过登录上下文动态获取。
- [ ] **优化案例库回退逻辑**:
    - **问题**: 当 API 失败时，`index.vue` 可能会在没有提示的情况下显示旧的或模拟数据。
    - **任务**: 实现标准的加载中/空数据状态，并清理不必要的模拟数据触发。

## 🟢 低优先级 (改进与工具)

- [ ] **扩展 API 客户端工具**:
    - **任务**: 为 `scripts/api-client.js` 添加更多测试用例，涵盖边界情况和参数验证。

- [ ] **代码清理**:
    - **任务**: 检查并移除冗余遗留逻辑（例如 `surveys.vue` 中未使用的 `queryQWTaskFile` 方法）。

## 📸 图片与静态资源优化 (Image Optimization)

- [ ] **[高优先级] 全局图片组件封装**:
    - **任务**: 封装一个统一的 `<c-image>` 组件，内部集成 `getImgUrl` 逻辑、骨架屏加载占位符 (Skeleton) 和错误图兜底 (Fallback)，替换原生的 `<image>` 标签，彻底避免手动拼接 URL。
- [ ] **[中优先级] 本地静态资源瘦身**:
    - **任务**: 扫描 `/static` 目录，找出大于 50KB 的本地图片，压缩或上传至 CDN，减少小程序包体积（目前小程序包体积可能有进一步压缩空间）。
- [ ] **[低优先级] 头像裁剪功能**:
    - **任务**: 用户上传头像时，在前端或利用七牛云 API 进行自动裁剪，避免上传 5MB 原图只为了显示 100px 头像。
